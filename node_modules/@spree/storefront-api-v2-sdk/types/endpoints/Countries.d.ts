import Http from '../Http';
import type { ICountriesResult, ICountryResult, DefaultOptions, ListOptions, ShowOptions } from '../interfaces/Country';
import type { IQuery } from '../interfaces/Query';
export default class Countries extends Http {
    list(options?: ListOptions): Promise<ICountriesResult>;
    show(options: ShowOptions): Promise<ICountryResult>;
    /**
     * @deprecated Use the combined options signature instead.
     */
    show(iso: string, params: IQuery): Promise<ICountryResult>;
    default(options?: DefaultOptions): Promise<ICountryResult>;
    /**
     * @deprecated Use the combined options signature instead.
     */
    default(params: IQuery): Promise<ICountryResult>;
}
