import { Options as PrettierOptions } from 'prettier';
export declare function formatPrettier(defaultOptions: Omit<PrettierOptions, 'parser'> & Required<Pick<PrettierOptions, 'parser'>>): {
    format(input: string): Promise<string>;
};
