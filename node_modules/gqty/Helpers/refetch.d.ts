import type { InnerClientState } from '../Client/client';
import type { Resolvers } from '../Client/resolvers';
export declare function isFunction<T>(v: T | (() => T)): v is () => T;
export interface Refetch {
    <T = void | undefined>(refetchArg: T | (() => T)): Promise<T>;
}
export declare function createRefetch(innerState: InnerClientState, resolveSelections: Resolvers['resolveSelections'], inlineResolved: Resolvers['inlineResolved']): Refetch;
