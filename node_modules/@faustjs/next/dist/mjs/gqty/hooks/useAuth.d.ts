import type { RequiredSchema } from '@faustjs/react';
import type { NextClient } from '../client.js';
export interface UseAuthOptions {
    /**
     * Specify if the useAuth hook should facilitate the redirect to the appropriate url.
     *
     * @default true
     * @type {boolean}
     * @memberof UseAuthOptions
     */
    shouldRedirect?: boolean;
}
export declare function create<Schema extends RequiredSchema, ObjectTypesNames extends string = never, ObjectTypes extends {
    [P in ObjectTypesNames]: {
        __typename?: P;
    };
} = never>(): NextClient<Schema, ObjectTypesNames, ObjectTypes>['auth']['useAuth'];
