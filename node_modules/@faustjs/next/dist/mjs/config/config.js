import defaults from 'lodash/defaults.js';
let nextConfig = {
    revalidate: 900,
};
export function config(cfg) {
    nextConfig = defaults({}, cfg, nextConfig, {
        revalidate: 900,
    });
    return nextConfig;
}
